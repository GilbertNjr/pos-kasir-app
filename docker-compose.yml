version: '3.8'

services:
  pos-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pos_kasir_app
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - JWT_SECRET=pos_kasir_secret_key_production_2026
    restart: always
